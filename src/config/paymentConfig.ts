// Fill in the real UPI ID once you have it (e.g. "chromaticpoint@okhdfcbank").
// Until then this generates a valid-looking QR that simply won't resolve to
// a real payee — the invoice layout and QR sizing are already final, so
// swapping this one string is the only change needed later.
export const UPI_ID = "your-upi-id@bank";
export const PAYEE_NAME = "Chromatic Point";
